# Rust build output for the operator TUI crate (tui/, task T-102). Source, Cargo.toml and the
# committed Cargo.lock are tracked; only the compiled target/ tree is ignored (mirrors the
# engine/target/ rule in the repo-root .gitignore).
/target/
